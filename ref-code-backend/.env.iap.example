# IAP Configuration Environment Variables
# Copy this file to .env.iap and fill in your values
# These variables are required for IAP authentication to work

# GCP Project Number (numeric ID, not project ID)
# Get it with: gcloud projects describe adk-rag-ma --format="value(projectNumber)"
PROJECT_NUMBER=123456789

# Backend Service ID from Load Balancer
# Get it with: gcloud compute backend-services describe <backend-service-name> --global --format="value(id)"
# NOTE: In multi-agent setups, each Cloud Run backend service (backend, backend-agent1, etc.)
# has its own unique BACKEND_SERVICE_ID from its corresponding LB backend service.
# The CI/CD pipeline discovers and injects these automatically per service.
BACKEND_SERVICE_ID=9876543210123456789

# Optional: Override default database path
# DATABASE_PATH=/path/to/users.db

# Optional: Set log level
# LOG_LEVEL=INFO

# Note: The IAP_AUDIENCE is constructed automatically from PROJECT_NUMBER and BACKEND_SERVICE_ID
# Format: /projects/{PROJECT_NUMBER}/global/backendServices/{BACKEND_SERVICE_ID}
